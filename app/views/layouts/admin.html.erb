<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Admin - Bus Ticket" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Include Tailwind CSS %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <%# Sidebar Navigation %>
    <aside class="fixed inset-y-0 left-0 w-64 bg-blue-700 text-white">
      <div class="p-6 border-b border-blue-600">
        <h1 class="text-xl font-bold">Admin Dashboard</h1>
        <div data-controller="clock" class="text-sm text-blue-200 mt-2"></div>
      </div>

      <%# Navigation Links %>
      <nav class="mt-6">  
        <%= link_to admin_bus_schedules_path,
            class: "flex items-center px-6 py-3 hover:bg-blue-600" do %>
          <span>Bus Schedules</span>
        <% end %>

        <%= link_to admin_users_path,
            class: "flex items-center px-6 py-3 hover:bg-blue-600" do %>
          <span>Users</span>
        <% end %>

        <%= link_to admin_bookings_path,
            class: "flex items-center px-6 py-3 hover:bg-blue-600" do %>
          <span>Bookings</span>
        <% end %>
      </nav>

      <%# Back to Main Site Link %>
      <div class="absolute bottom-0 left-0 right-0 p-6 border-t border-blue-600">
        <%= link_to logout_admin_bus_schedules_path, data: { turbo_method: :delete }, class: "flex items-center text-blue-200 hover:text-white" do %>
          <span class="mr-2">←</span>
          <span>Log Out</span>
        <% end %>
      </div>
    </aside>
z
    <%# Main Content Area %>
    <main class="ml-64 min-h-screen">
      <%# Top Header %>
      <header class="bg-white shadow-sm border-b border-gray-200 px-8 py-4">
        <h2 class="text-xl font-semibold text-gray-800">
          <%= content_for(:page_title) || "Bus Schedules" %>
        </h2>
      </header>

      <%# Flash Messages %>
      <% if notice %>
        <div class="mx-8 mt-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded-lg">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="mx-8 mt-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded-lg">
          <%= alert %>
        </div>
      <% end %>

      <%# Page Content %>
      <div class="p-8">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
